# External coder operating contract for Template Mind

⬡B:docs.specs.external_coders_jds:JD:true_zero_pointer:20260730⬡

The full external-coder organization doctrine has one canonical home in the `anew` repository.
It is not copied into Template Mind because this repository is inherited by every world and must
remain a TRUE ZERO with no real-person context.

For work in this repository:

- CATHY is the independent external SHADOW auditor. She verifies source, callers, tests, protected
  branch state, deployment state where a live world exists, and exact readback. A claimed fix without
  those gates remains open.
- External findings route through the CCWA Command Center to A'NEW and CODA. No external coder
  impersonates the internal mind or reaches a human from product code.
- Product identity, provider selection, model selection, keys, and world ownership resolve at runtime.
- Audit evidence may name repository paths and immutable SHAs. It may not copy real-person identity,
  credentials, or world-specific doctrine into this template.
- If the canonical JD cannot be read, report `ok:false` for organization-doctrine provenance and
  continue only the safe source checks that do not require guessing.

This file is a TRUE ZERO local contract and a one-source pointer. It does not twin the full doctrine.

⬡B:docs.specs.external_coders_jds:SEAL:true_zero_pointer:20260730⬡
